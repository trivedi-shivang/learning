// given a graph where starting point is 0,0 and ending point is width-1, height-1, find how many I can traverse from starting to ending point.
// you can only traverse one box right or down. you cannot traverse
function numberOfWaysToTraverseGraph(width, height) {
  //given
}
numberOfWaysToTraverseGraph(4, 3);
